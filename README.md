# OpenScienceRAG
A minimal demo implementation of a RAG system that uses OpenAlex API to get scientific papers and answer small queries accordingly.


## Current TODO:
- [ ] Implementing the Query Translation
    - [ ] Define a pipeline [User -> LLM -> Query(s) fields -> Open Alex Query(s)]
    - [ ] Structure the LLM output for the query